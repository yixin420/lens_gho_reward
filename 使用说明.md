# 长毛象中文站爬虫使用说明

## 项目简介

这是一个用于爬取长毛象中文站 (https://m.cmx.im/public/local) 近两个月中文帖子的Python爬虫工具。

## 功能特性

- ✅ 自动爬取长毛象公共时间线的帖子
- ✅ 智能识别中文内容（中文字符占比>30%）
- ✅ 时间过滤（近两个月内的帖子）
- ✅ 提取帖子内容、用户名、发布时间和链接
- ✅ 支持CSV和JSON格式导出
- ✅ 详细的日志记录
- ✅ 自动处理页面滚动加载

## 环境要求

- Python 3.7+
- Chrome浏览器
- 稳定的网络连接

## 安装步骤

### 1. 克隆或下载项目文件

确保您有以下文件：
- `mastodon_scraper.py` - 主爬虫脚本
- `test_scraper.py` - 测试脚本
- `requirements.txt` - 依赖包列表

### 2. 安装Python依赖

```bash
pip install -r requirements.txt
```

### 3. 确保Chrome浏览器已安装

爬虫使用Selenium和Chrome WebDriver，会自动下载匹配的ChromeDriver。

## 使用方法

### 1. 运行测试脚本（推荐）

首次使用前，建议先运行测试脚本检查环境：

```bash
python test_scraper.py
```

测试脚本会检查：
- 网络连接
- 中文检测功能
- Selenium WebDriver设置

### 2. 运行主爬虫脚本

```bash
python mastodon_scraper.py
```

### 3. 查看结果

爬虫运行完成后，会在以下位置生成文件：

- `data/raw/mastodon_posts_YYYYMMDD_HHMMSS.csv` - CSV格式数据
- `data/raw/mastodon_posts_YYYYMMDD_HHMMSS.json` - JSON格式数据
- `mastodon_scraper.log` - 详细日志文件

## 输出数据格式

### CSV/JSON字段说明

- `username`: 用户名
- `content`: 帖子内容
- `time`: 原始时间文本
- `parsed_time`: 解析后的ISO格式时间
- `url`: 帖子链接

### 示例数据

```json
{
  "username": "@user123",
  "content": "今天天气真好！适合出去走走。",
  "time": "2小时前",
  "parsed_time": "2024-10-09T10:30:00",
  "url": "https://m.cmx.im/@user123/123456789"
}
```

## 配置选项

您可以在 `mastodon_scraper.py` 中修改以下配置：

```python
# 时间过滤范围（天数）
self.cutoff_date = datetime.now() - timedelta(days=60)  # 默认60天

# 中文内容判断阈值
chinese_char_count / total_chars > 0.3  # 默认30%

# 最大滚动次数
max_scrolls: int = 50  # 默认50次
```

## 注意事项

### 1. 使用限制

- 请遵守网站的robots.txt和使用条款
- 建议设置合理的请求间隔，避免对服务器造成过大压力
- 本工具仅用于学习和研究目的

### 2. 可能遇到的问题

**问题1：ChromeDriver下载失败**
```
解决方案：
- 检查网络连接
- 手动下载ChromeDriver并设置PATH
- 使用VPN或代理
```

**问题2：页面加载超时**
```
解决方案：
- 检查网络连接
- 增加等待时间
- 重新运行脚本
```

**问题3：找不到帖子元素**
```
解决方案：
- 网站可能更新了页面结构
- 检查CSS选择器是否需要更新
- 运行测试脚本诊断问题
```

### 3. 性能优化建议

- 在网络条件良好时运行
- 避免在网站高峰期使用
- 可以分批次运行，避免一次性爬取过多数据

## 技术细节

### 爬虫工作流程

1. 启动Chrome WebDriver（无头模式）
2. 访问目标页面
3. 滚动页面加载更多内容
4. 解析页面DOM提取帖子信息
5. 过滤中文内容和时间范围
6. 保存数据到文件

### 中文检测算法

使用正则表达式 `[\u4e00-\u9fff]+` 匹配中文字符，计算中文字符占比：

```python
chinese_char_count / total_chars > 0.3
```

### 时间解析

支持多种时间格式：
- 相对时间：N分钟前、N小时前、N天前、N周前、N月前
- 绝对时间：YYYY-MM-DD HH:MM:SS等格式

## 更新日志

- v1.0.0 (2024-10-09)
  - 初始版本发布
  - 支持基本爬取功能
  - 中文内容过滤
  - 时间范围过滤
  - CSV/JSON导出

## 许可证

本项目仅供学习和研究使用，请遵守相关法律法规和网站使用条款。

## 支持

如果遇到问题，请：
1. 首先运行测试脚本诊断环境
2. 查看日志文件获取详细错误信息
3. 检查网络连接和Chrome浏览器版本